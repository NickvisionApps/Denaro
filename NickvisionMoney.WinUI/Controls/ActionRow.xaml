<wct:DockPanel
    x:Class="NickvisionMoney.WinUI.Controls.ActionRow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:NickvisionMoney.WinUI.Controls"
    xmlns:nickvision="using:NickvisionMoney.WinUI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:wct="using:CommunityToolkit.WinUI.UI.Controls"
    xmlns:labs="using:CommunityToolkit.Labs.WinUI"
    mc:Ignorable="d" Margin="6" LastChildFill="False">

    <StackPanel wct:DockPanel.Dock="Left" Orientation="Vertical">
        <TextBlock Text="{x:Bind Title, Mode=OneWay}"/>

        <TextBlock Style="{ThemeResource CaptionTextBlockStyle}" Foreground="Gray" Text="{x:Bind Subtitle, Mode=OneWay}"/>
    </StackPanel>
</wct:DockPanel>
