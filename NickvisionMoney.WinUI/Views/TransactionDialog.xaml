<ContentDialog
    x:Class="NickvisionMoney.WinUI.Views.TransactionDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:NickvisionMoney.WinUI.Views"
    xmlns:nickvision="using:NickvisionMoney.WinUI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:wct="using:CommunityToolkit.WinUI.UI.Controls"
    xmlns:labs="using:CommunityToolkit.Labs.WinUI"
    mc:Ignorable="d" Style="{StaticResource DefaultContentDialogStyle}"
    DefaultButton="Primary">

    <StackPanel Spacing="6" Width="400">
        <Border Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}" BorderThickness="1" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" CornerRadius="8">
            <StackPanel Margin="12" Spacing="6">
                <TextBox x:Name="TxtDescription" HorizontalAlignment="Stretch" IsSpellCheckEnabled="True"/>

                <TextBox x:Name="TxtAmount" HorizontalAlignment="Stretch"/>

                <ComboBox x:Name="CmbType" HorizontalAlignment="Stretch"/>
            </StackPanel>
        </Border>

        <Border Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}" BorderThickness="1" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" CornerRadius="8">
            <StackPanel Margin="12" Spacing="6">
                <CalendarDatePicker x:Name="CalendarDate" HorizontalAlignment="Stretch"/>

                <ComboBox x:Name="CmbRepeatInterval" HorizontalAlignment="Stretch"/>
            </StackPanel>
        </Border>

        <Border Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}" BorderThickness="1" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" CornerRadius="8">
            <StackPanel Margin="12" Spacing="6">
                <ComboBox x:Name="CmbGroup" HorizontalAlignment="Stretch"/>

                <TextBlock x:Name="LblColor"/>

                <wct:ColorPickerButton x:Name="BtnColor" HorizontalAlignment="Stretch"/>
            </StackPanel>
        </Border>

        <TextBlock Name="TxtErrors" Visibility="Collapsed" Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"/>
    </StackPanel>
</ContentDialog>
