using Gtk 4.0;

Gtk.Overlay _root {
  child: Gtk.Image _colorImage {
    overflow: hidden;
    pixel-size: 1;
    halign: center;
    valign: center;

    styles ["id-image"]
  };

  [overlay]
  Gtk.Label _idLabel {
    use-markup: true;
  }
}